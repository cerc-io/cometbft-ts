// TODO: Generate

// SignedMsgType is a type of signed message in the consensus.
export type SignedMsgType = number

// Vote represents a prevote, precommit, or commit vote from validators for
// consensus.
export class Vote {}

export class Proposal {}
